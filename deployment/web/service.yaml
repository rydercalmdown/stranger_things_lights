apiVersion: v1
kind: Service
metadata:
  name: stranger-things-lights-web
spec:
  ports:
    - name: https
      protocol: TCP
      port: 80
      targetPort: 5000
  selector:
    app: stranger-things-lights-web
